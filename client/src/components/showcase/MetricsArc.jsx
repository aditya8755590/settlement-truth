import { motion } from "framer-motion";
export default function MetricsArc({ audit }) {
  const stats = audit.hasAudit ? [[audit.total, "Records processed"], [`${audit.matchRate}%`, "Evidence-backed match rate"],[audit.exceptions,"Exceptions retained"],[0,"Forced matches"]] : [["—","Records processed"],["—","Match rate"],["—","Exceptions"],[0,"Forced matches"]]; return <section className="bg-[#eef5ff] px-5 pb-24"><div className="mx-auto max-w-5xl overflow-hidden rounded-[36px] border border-blue-100 bg-white/60 p-8 text-center shadow-xl shadow-blue-950/5 backdrop-blur-xl sm:p-14"><div className="mx-auto mb-[-5rem] size-80 rounded-t-full border-[1.5px] border-dashed border-blue-300/80"><motion.i className="relative left-[55%] top-8 block size-3 rounded-full bg-blue-600 shadow-[0_0_18px_#2563eb]" animate={{x:[0,70,0],y:[0,36,0]}} transition={{duration:4,repeat:Infinity}}/></div><p className="relative text-xs font-bold uppercase tracking-[.2em] text-blue-700">Numbers speak, evidence decides</p><div className="relative mt-7 grid gap-4 sm:grid-cols-4">{stats.map(([value,label])=><div key={label}><strong className="block text-3xl tracking-[-.05em] text-slate-950">{value}</strong><span className="mt-2 block text-xs text-slate-500">{label}</span></div>)}</div></div></section>; }

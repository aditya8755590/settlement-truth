import { AnimatePresence, motion } from "framer-motion"; import { Plus } from "lucide-react"; import { useState } from "react";
const items=[["Does AI decide whether a payment matches?","No. The deterministic engine evaluates identifiers, amounts, settlements, bank credits, and policy checks. AI may only explain its output."],["What happens when evidence is incomplete?","The record stays in review. Settlement Truth does not force a match from timing, similarity, or a plausible amount."],["Which sources can be reconciled?","Orders, gateway payments, refunds, settlement reports, and bank-credit records can be normalized into one evidence trail."]];
export default function FAQ(){const [open,setOpen]=useState(0);return <section className="bg-[#eef5ff] px-5 py-24"><div className="mx-auto max-w-3xl"><p className="text-center text-xs font-bold uppercase tracking-[.2em] text-blue-700">Questions, answered precisely</p><h2 className="mt-3 text-center text-4xl font-semibold tracking-[-.05em]">No mystery layer.</h2><div className="mt-10 space-y-3">{items.map(([q,a],i)=><div key={q} className="rounded-[24px] border border-blue-100 bg-white/70 px-5 shadow-sm"><button onClick={()=>setOpen(open===i?-1:i)} className="flex w-full items-center justify-between gap-4 py-5 text-left font-medium"><span>{q}</span><Plus className={open===i?'rotate-45 transition':'transition'} size={18}/></button><AnimatePresence>{open===i&&<motion.p initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden pb-5 text-sm leading-6 text-slate-600">{a}</motion.p>}</AnimatePresence></div>)}</div></div></section>}
